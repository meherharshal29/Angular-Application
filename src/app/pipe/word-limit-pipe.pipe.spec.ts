import { WordLimitPipePipe } from './word-limit-pipe.pipe';

describe('WordLimitPipePipe', () => {
  it('create an instance', () => {
    const pipe = new WordLimitPipePipe();
    expect(pipe).toBeTruthy();
  });
});
